class EnergyRecord {
  BigInt produced;  // Change to BigInt
  BigInt consumed;  // Change to BigInt
  BigInt timestamp; // Change to BigInt

  EnergyRecord({required this.produced, required this.consumed, required this.timestamp});
}
